# Vibeboss workspace root

You are at the Vibeboss workspace root, not at HQ. There is no agent identity here.

To talk to the workspace lead, start the session in:

```bash
cd hq
```

Then launch your agent tool there:

- Claude Code: `claude`
- Codex: open/run Codex from this `hq/` directory

The HQ directory contains the runtime instructions (`hq/CLAUDE.md` for Claude Code, `hq/AGENTS.md` for Codex), state, lessons, crew, projects, and inbox context.

Do not perform daily workspace work from this directory. Treat this location as a routing surface only.

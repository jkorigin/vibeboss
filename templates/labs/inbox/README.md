# Labs Inbox

Incoming research requests. {{LAB_LEAD_NAME}} reads `requests/` on every spawn.

```
inbox/
  requests/    ← incoming research requests (from Boss or build agents)
  processed/   ← completed requests (moved here after pickup)
```

# Handoffs

Audit log of research dispatched to build agents. One `.md` file per day: `YYYY-MM-DD.md`.

**Format per entry:**
```
YYYY-MM-DD HH:MM — [<topic>] → <target-agent> (<project>) — <one-line headline>
```

This directory is append-only. Never edit past entries.

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Secrets — never commit
.env
.env.*
!.env.example

# Runtime memory lives in a separate ../vibeboss-workspace/ directory (not in this repo).
# See CLAUDE.md for the topology rationale. Nothing runtime should appear here at all;
# if you find runtime files inside vibeboss/, they're misplaced — move them to the workspace.

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo

# Editor-specific launch configs (machine/project-specific, not framework canon)
.claude/launch.json

# Logs / scratch
*.log
tmp/
.cache/

# Runtime tracking — list of workspaces this source has scaffolded (machine-specific, never canon).
.workspaces

# Sensitivity denylist — local-only literal runtime terms (contains the data it protects; never commit)
.vibeboss-denylist
